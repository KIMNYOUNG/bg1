// 이 파일은 현재 비어 있습니다.
// 향후 웹페이지에 동적인 기능을 추가하고 싶을 때 여기에 JavaScript 코드를 작성할 수 있습니다.
// 예시:
/*
document.addEventListener('DOMContentLoaded', () => {
    const lessonSections = document.querySelectorAll('.lesson-section h3');

    lessonSections.forEach(header => {
        header.style.cursor = 'pointer';
        header.addEventListener('click', () => {
            const content = header.nextElementSibling; // Get the next sibling element (ul or ol)
            if (content) {
                content.style.display = content.style.display === 'none' ? 'block' : 'none';
            }
        });
    });
});
*/